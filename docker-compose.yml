services:
  togewire:
    image: ghcr.io/kurodaze/togewire:latest
    container_name: togewire
    restart: unless-stopped
    ports:
      - "7093:7093"
    volumes:
      - ./configs:/app/configs
      - ./data:/app/data
