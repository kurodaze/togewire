services:
  togewire:
    image: ghcr.io/kurodaze/togewire:latest
    container_name: togewire
    restart: unless-stopped
    network_mode: host
    user: 1000:1000
    volumes:
      - ./configs:/app/configs
      - ./data:/app/data
